# resources/

Extra resources copied verbatim into the packaged app as `desktop-resources/`
(see the `extraResources` entry in `package.json`). Empty in the first cut; the
tray icon and any bundled assets the shell loads at runtime land here.
